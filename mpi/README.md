# MPI

There are three implementations of MPI on the ARC clusters:

1. OpenMPI.  Examples are in directory _open_mpi_.
2. MPICH.  Example is in directory _mpich_.
3. Intel MPI.  Example is in directory _intel_mpi_.

At least one example is in each directory.
